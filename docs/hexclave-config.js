// Hexclave configuration for the SwiperNoSwiping site.
// Create a project at https://app.hexclave.com, then paste the PROJECT ID here.
// Client-side gets the project ID ONLY — the HEXCLAVE_SECRET_SERVER_KEY never
// belongs in this file or anywhere the browser can see (server/.env only).
window.HEXCLAVE_PROJECT_ID = "";
